# 代码规范

## import 规范

在从外部库进行导入时候，请遵循以下顺序：

对于标准库和第三方库的导入，请按照如下顺序：

- 需要使用 `from ... import ...` 语法的导入放在前面。
- 直接使用 `import ...` 语法的导入放在后面。
- 对于使用 `from ... import ...` 导入的多个项，请在保证不会引起 import 错误的前提下，按照字母顺序排列。
- 对于使用 `import ...` 导入的多个项，请在保证不会引起 import 错误的前提下，按照字母顺序排列。

对于本地模块的导入，请按照如下顺序：

- 对于同一个文件夹下的模块导入，使用相对导入，排列顺序按照不发生 import 错误的前提下，随便排列。
- 对于不同文件夹下的模块导入，使用绝对导入。这些导入应该以 `from src` 开头，并且按照不发生 import 错误的前提下，尽量使得第二层的文件夹名称相同的导入放在一起；第二层文件夹名称排列随机。
- 标准库和第三方库的导入应该放在本地模块导入的前面。
- 各个导入块之间应该使用一个空行进行分隔。
- 对于现有的代码，如果导入顺序不符合上述规范，在重构代码时应该调整导入顺序以符合规范。

## 注释规范

### 总则：解释"为什么"，而非复述"做了什么"

- 注释应解释代码背后的业务逻辑、设计意图或非显而易见的约束，禁止仅重复代码字面含义的废话注释（如 `i++  # i 自增 1`）。
- 尽量保持良好的注释。

### 文件级注释

每个源代码文件顶部必须添加多行文档注释（语言等效语法，如 Python 模块 docstring、Javadoc、Doxygen 等），清晰说明：

- 文件的总体功能与所属模块；
- 核心职责及对外暴露的关键接口；
- 与其他文件/模块的依赖关系（导入来源、被谁调用）；
- 禁止模糊描述，须具体到业务逻辑或技术实现层面。

### 方法/函数级注释

每个公开方法及关键私有方法前必须添加多行文档注释，包含：

- 精确的功能描述（避免重复方法名）；
- 所有参数的类型、含义、取值范围及默认值（若适用）；
- 返回值类型及语义说明；
- 可能抛出的异常或错误条件；
- 重要的副作用或性能注意事项。

### 行内注释

- 仅在复杂逻辑、算法步骤或非直观代码处使用单行注释，解释"为什么"而非"做什么"。
- 禁止对显而易见的代码添加冗余注释。

### 重构时的注释处理

- 如果原来的代码中有注释，则重构的时候，除非这部分代码被删除，否则相同功能的代码应该保留注释（可以对注释进行修改以保持准确性，但不应该删除注释）。
- 若原注释口语化严重但包含有效信息（如踩坑经验、行为差异），必须重写为专业表述，不得直接丢弃。
- 若原注释已过时或错误，应更新或删除，并在 commit message 中说明原因。

### 无注释代码的补充

- 如果原来的代码中没有注释，则重构的时候，如果某个功能块的代码较长或者逻辑较为复杂，则应该添加注释来解释这部分代码的功能和逻辑。

### 踩坑经验必须留痕

涉及底层协议、并发竞态、异步状态机、第三方库特定行为的代码，注释必须包含三要素：

| 要素 | 要求               | 示例                                                         |
| ---- | ------------------ | ------------------------------------------------------------ |
| 现象 | 错误写法会触发什么 | `accept()` 之后 `close()` 会触发 undici Warning 及 receive 报错 |
| 原因 | 底层机制是什么     | `accept()` 之前 `close()` 在 ASGI 层面等同于直接断开 TCP     |
| 后果 | 擅自修改的风险     | 客户端须收到 1002 Protocol Error 才能按预期停止重连          |

### 风格与语气

- 专业、简洁、无歧义，使用主动语态和技术术语；避免情绪化与过度口语化的表述：
  - 反例：`# 客户端会老老实实停止重连`
  - 正例：`# 使客户端收到 1002 Protocol Error，按预期停止重连`
- 注释内容需独立于代码可读，仅通过注释即可理解文件结构与方法行为。
- 注释语言遵循【语言规范】，首选简体中文；函数名、协议名、错误码等技术术语保留英文原文，不强行翻译。
- 因果链条较长时，分点结构化书写，提高可读性。
- 全项目注释风格、缩进、标签格式必须统一，遵循目标语言的官方文档注释规范（如 Javadoc、Doxygen、Sphinx、PEP 257 等）。

### 特殊场景

- 允许使用 `TODO`、`FIXME`、`NOTE`、`WORKAROUND` 等标准标记标注待办事项、临时绕过方案或注意事项；临时绕过方案须注明绕过的问题与预期修复条件，有对应 issue 链接的应附上。
- **严禁**出现 `M1`、`M2`、`Phase1`、`Stage A` 等任何阶段性、里程碑式标识符。
- **严禁**使用 emoji、表情符号或非正式表达。
- 公共类、公共方法应包含 Docstring：功能、参数含义、返回值、可能抛出的异常。
- 配置项与常量必须注明含义、单位、取值范围。

### 注释与代码同步

- 逐文件处理，确保注释与代码逻辑完全同步，不遗漏、不臆造。
- 批量补充或重构注释完成后，须在 commit message 或 PR 描述中提供修改摘要。

### 示例

以下三个示例针对同一处逻辑（鉴权失败后必须在 `accept()` 之前关闭连接），演示三种可选风格，按团队偏好择一即可。

#### 方案一：标准工程风格

结构清晰，保留完整的技术因果链条，适合大多数场景。

```python
# 【关键】鉴权与关闭连接必须在 accept() 之前执行。
#
# 原因：
# 1. 若在 accept() 之后调用 close()，会触发完整的 WebSocket 握手后关闭流程。
#    这会导致 undici 客户端记录 Warning 日志，并出现 "accept后拒绝" 的状态不一致，
#    进而导致 receive 阶段报错，极难排查。
# 2. 在 accept() 之前调用 close()，在 ASGI 层面等同于直接断开 TCP 连接。
#    客户端将收到 1002 Protocol Error，从而按预期停止重连。
```

#### 方案二：极简警示风格

用最短的文字说明风险，适合行宽受限或偏好简洁的团队。

```python
# 严禁将 close() 移至 accept() 之后。
# 否则 undici 客户端会因 "握手后断开" 触发 Warning 及状态不一致错误。
# 当前写法 (accept前关闭) 会触发 TCP 层断开 (1002 Protocol Error)，确保客户端正确停止重连。
```

#### 方案三：结构化 / 标签风格

使用标签（Tag）索引问题，方便后续搜索，适合大型项目。

```python
# [WORKAROUND] Undici Client Compatibility Issue
#
# 必须在 accept() 前执行鉴权失败后的 close()。
# - 现象: accept() 后 close() 会导致 undici 抛出 Warning 且 receive 报错。
# - 原理: accept() 前 close() = TCP RST/FIN (1002 Error)，客户端行为确定。
# - 参考: 见 undici issue #xxxx (如有具体 issue 链接应附上)
```

> 三种方案的共同底线：**现象 / 原因 / 后果三要素缺一不可**，技术术语（`accept`、`close`、`undici`、`ASGI`、`1002 Protocol Error`）保留英文原文。

## 类型注解规范

- 重构代码时，如果原来的代码中有类型注解，则相同功能的代码应该保留类型注解（可以对类型注解进行修改以保持准确性，但不应该删除类型注解）。
- 重构代码时，如果原来的代码中没有类型注解，则重构的时候，如果某个函数的功能较为复杂或者参数较多，则应该添加类型注解来提高代码的可读性和可维护性。（对于简单的变量，可以不添加类型注解）
- 对于参数化泛型，应该使用`typing`模块中的类型注解来指定参数化泛型的类型。
  - 例如，使用`List[int]`来表示一个包含整数的列表，使用`Dict[str, Any]`来表示一个键为字符串，值为任意类型的字典。

## 变量规范

- 当确定某个变量/实例是某种类型的时候（优先按照类型注解确定，除非你分析出类型注解是错误的），可以不必使用`or`进行fallback。
  - 例如，`bot_nickname = (global_config.bot.nickname or "").strip()` 可以改为 `bot_nickname = global_config.bot.nickname.strip()`，前提是我们确定`global_config.bot.nickname`一定是一个字符串。

## 类属性使用规范

- 应该尽量减少使用getattr和setattr方法，除非是在对一个动态类进行处理或者使用Monkeypatch完成Pytest
- 在重构代码时，如果遇到getattr和setattr，应该尝试检查这个类实例是否有这个属性，如果有，则直接替换为类属性访问写法。
  - 举例：`v = getattr(instance, "value", "")` 在检查到`instance`有`value`属性后应该改为`v = instance.value`

## debug 规范

- 不要总是想找兜底，一定要精准的找到问题的核心，然后提出建议，兜底是不合适，难以维护的。
- 不要总是考虑 fallback，如果哪里有错误，一定要让他及时完整的暴露，而不是用 fall_back 兜底掩盖过去。

## 运行/调试/构建/测试/依赖

- 优先使用 uv，依赖项以 `pyproject.toml` 为准，要同步更新 `requirements.txt`。
- 不要总是考虑 fallback，如果哪里有错误，一定要让他及时完整的暴露，而不是用 fall_back 兜底掩盖过去。

## 语言规范

- 项目的首选语言为简体中文，无论是注释语言，日志展示语言，还是 WebUI 展示语言都首要以简体中文为首要实现目标。

## 借鉴规范

在参考、借鉴、移植其他项目的代码或设计思路时，必须遵守以下规则：

- 禁止在代码注释中出现被借鉴项目的名称、作者名、仓库地址、Issue 编号、PR 链接或任何可追溯到原项目的标识信息。
- 禁止在文档（README、设计文档、变更日志等）的描述中出现被借鉴项目的名称、链接或归属说明。
- 禁止在变量名、函数名、类名、文件名中残留被借鉴项目的命名痕迹（如以原项目名作为前缀或后缀）。
- 借鉴的代码必须经过理解后重新实现，使其风格与本项目的代码规范保持一致，而非直接复制粘贴后仅做表面修改。
- 如果借鉴的内容涉及特定许可证义务（如 GPL、AGPL 等 copyleft 协议），必须在引入前向团队明确说明，经确认后方可使用，且须遵守相应许可证的要求。

> **禁止在代码中留下 AI 隐性标记！！**
> **禁止在 Git 中留下 AI 隐性标记！！**

## Commit 规范

- commit message 必须使用简体中文书写。

- commit message 遵循 Conventional Commits 格式，结构为：

  ```
  <type>(<scope>): <description>
  ```

  其中：

  - type 取值为：feat（新功能）、fix（修复缺陷）、refactor（重构）、docs（文档）、style（格式调整）、test（测试）、chore（构建/工具/依赖）、perf（性能优化）。
  - scope 为可选项，填写受影响的模块或文件名。
  - description 应简洁明确地描述本次变更的内容和目的，不超过 72 个字符。

- 每次 commit 应当是原子性的，即一个 commit 只包含一个逻辑上完整的变更，禁止将多个不相关的改动混在同一次提交中。

- 禁止在 commit message 中包含任何 AI 生成标记、AI 工具署名或类似暗示性文本。

- 禁止在 commit 的 author、committer 信息中留下 AI 相关标记。

- 提交前必须确保代码可正常运行/构建，禁止提交明显无法通过基本验证的代码。

- 对于大型重构或多步骤变更，应拆分为多个有意义的小 commit，每个 commit 对应一个可独立理解的变更单元，而非一次性提交所有改动。

## 禁止提交的内容

以下内容严禁被加入版本控制，提交前必须确认不涉及任何一项：

- **密钥与凭证**：包括但不限于 API Key、Secret Key、Token、密码、证书文件（`.pem`、`.key`、`.p12` 等）。任何包含真实凭证的文件都不得提交。
- **真实配置文件**：包含实际运行环境配置的文件（如 `.env`、`config.local.yaml`、`settings.local.py` 等）禁止提交。如需提供配置模板，应提交不含真实值的 `.env.example` 或 `config.example.yaml`。
- **本机环境文件**：包括本机路径、本机特有的环境变量配置、本地数据库连接串等与开发者个人环境绑定的内容。
- **IDE / 编辑器目录**：`.idea/`、`.vscode/`（除团队共享的 settings 外）、`*.iml`、`.swp`、`.swo` 等。
- **AI 工具目录**：`.claude/`、`.cursor/`、`.copilot/`、`.aider*` 等任何 AI 辅助工具生成的本地配置与缓存目录。
- **测试产物与临时文件**：pytest 缓存（`.pytest_cache/`）、覆盖率报告（`htmlcov/`、`.coverage`）、构建产物（`dist/`、`build/`、`*.egg-info/`）、`__pycache__/`、`*.pyc` 等。
- **日志与数据文件**：运行时产生的 `.log` 文件、本地数据库文件（`*.db`、`*.sqlite3`）、导出的临时数据文件。
- **操作系统文件**：`.DS_Store`、`Thumbs.db`、`desktop.ini` 等。
- **大型二进制 / 媒体文件**：未经团队确认的大型模型文件、视频、未压缩的图片资源等（应使用 Git LFS 或外部存储管理）。

必须确保 `.gitignore` 覆盖上述所有类别。若发现已有上述文件被误提交，应立即从版本历史中清除（使用 `git rm --cached` 或 `git filter-branch` / BFG），并更新 `.gitignore` 防止再次提交。